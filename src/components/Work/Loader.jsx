import React from 'react';

/**
 * A loader component. Shows up
 * when the data is still loading
 */
var Loader = React.createClass({
  render() {
    return (
      <div>
      </div>
    );
  }
});

export default Loader;